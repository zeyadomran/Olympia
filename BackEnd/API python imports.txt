These are the libraries I needed to install to run my api.

pip install Flask
pip install -U flask-cors
pip install -U app
pip install flask-mysql
pip install PyMySQL
pip install pyjwt

Please ensure the host and password in the matches your MySQL server in the config.py file.
Also ensure you have the database contents stored on that server.
